/**
 * Default limit for API requests when fetching a collection of items.
 */
export const LIMIT = 20
/**
 * Default page size for API requests when paginating through a collection of items.
 */
export const PAGE_SIZE = 50
